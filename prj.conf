# Console output via printk
CONFIG_PRINTK=y

# GPIO support for the LED and tamper switch
CONFIG_GPIO=y

# Interactive shell subsystem -- lets us register our own "tamper" command
CONFIG_SHELL=y

# shell owns the USB console
CONFIG_SHELL_BACKEND_SERIAL=y