# Available runners configured by board.cmake.
runners:
- blackmagicprobe
- jlink
- openocd
- probe-rs
- pyocd
- uf2

# Default flash runner if --runner is not given.
flash-runner: openocd

# Default debug runner if --runner is not given.
debug-runner: openocd

# Common runner configuration values.
config:
  board_dir: C:/Users/rcjam/zephyr/boards/raspberrypi/rpi_pico2
  # Build outputs:
  elf_file: zephyr.elf
  hex_file: zephyr.hex
  bin_file: zephyr.bin
  uf2_file: zephyr.uf2
  # Host tools:
  gdb: C:/Users/rcjam/zephyr-sdk-1.0.1/gnu/arm-zephyr-eabi/bin/arm-zephyr-eabi-gdb-py.exe
  openocd: C:/Users/rcjam/zephyr-sdk-1.0.1/hosttools/openocd/bin/openocd.exe
  openocd_search:
    - C:/Users/rcjam/zephyr-sdk-1.0.1/hosttools/openocd/share/openocd/scripts

# Runner specific arguments
args:
  blackmagicprobe:
    []

  jlink:
    - --dt-flash=y
    - --device=RP2350_M33_0
  openocd:
    - --cmd-load
    - flash write_image erase
    - --cmd-verify
    - verify_image
    - --cmd-pre-init
    - source [find interface/cmsis-dap.cfg]
    - --cmd-pre-init
    - source [find target/rp2350.cfg]
    - --cmd-pre-init
    - set_adapter_speed_if_not_set 5000
    - --gdb-pre-debug
    - monitor reset init
  probe-rs:
    - --chip=RP235x
  pyocd:
    - --dt-flash=y
    - --target=rp2350
  uf2:
    - --board-id=RP2350
